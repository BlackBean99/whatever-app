import 'package:flutter/material.dart';

class AppRadius {
  // Small radius values
  static const double r10 = 10.0;
  static const double r12 = 12.0;
  static const double r14 = 14.0;
  static const double r16 = 16.0;

  // Medium radius values
  static const double r20 = 20.0;

  // Large radius values
  static const double r28 = 28.0;

  // BorderRadius shortcuts for common use cases
  static final BorderRadius circular10 = BorderRadius.circular(r10);
  static final BorderRadius circular12 = BorderRadius.circular(r12);
  static final BorderRadius circular14 = BorderRadius.circular(r14);
  static final BorderRadius circular16 = BorderRadius.circular(r16);
  static final BorderRadius circular20 = BorderRadius.circular(r20);
  static final BorderRadius circular28 = BorderRadius.circular(r28);
}
